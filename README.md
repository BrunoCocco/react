# 🚀 Práctica con APIs en React

Este proyecto es parte de mi entrenamiento técnico para dominar el consumo de **APIs REST** utilizando **React**, `fetch()`, estados, efectos y manejo de errores.

El objetivo principal es aprender a:

- Consumir APIs reales en aplicaciones React.
- Utilizar `useEffect` correctamente para ejecutar llamadas a la API.
- Manejar estados de **loading**, **data** y **error**.
- Trabajar con **JSON**, interpretar su estructura y renderizar datos.
- Construir **URLs dinámicas** para endpoints que requieren parámetros.
- Practicar diferentes APIs públicas (Pokémon, CatFacts, CoinGecko, etc).

## 📂 Estructura del proyecto

- `src/App.jsx`  
  Contiene la lógica inicial de pruebas con `fetch()` y las primeras llamadas a APIs.
  
- `src/components/`  
  (Pendiente) Acá se irán creando componentes reutilizables para mostrar datos, cards y vistas dependiendo de las prácticas de cada día.

## 🧪 APIs Utilizadas

Durante esta práctica se consumen distintas APIs públicas:

- **Cat Facts**  
  `https://catfact.ninja/fact`

- **Pokémon API**  
  `https://pokeapi.co/api/v2/pokemon/ditto`

- **CoinGecko Ping**  
  `https://api.coingecko.com/api/v3/ping`

## 🎯 Objetivo general

Convertirme en un desarrollador capaz de:

- Integrar fuentes externas de datos en aplicaciones web.
- Entender cómo se estructura y manipula un JSON.
- Construir componentes dinámicos basados en datos reales.
- Prepararme para consumir APIs con autenticación, paginación y endpoints complejos.

## 🧠 Estado actual

Este repositorio se encuentra en proceso educativo.  
Cada archivo nuevo representa un paso del entrenamiento de 7 días centrado en **consumo de APIs**.

## 📌 Próximos pasos

- Implementar un sistema completo de `loading` y `error handling`.
- Crear un **Dashboard** con varias APIs.
- Practicar `POST`, `PUT`, `DELETE` utilizando APIs mock.
- Preparar llamadas a APIs autenticadas (tokens).

---

### ✨ Autor  
Bruuno – Full Stack Blockchain Developer (en progreso).
